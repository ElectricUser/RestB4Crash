1. Instalar a versão "2.1.3" do Flask:

pip install --upgrade --force-reinstall "Flask==2.1.3"

2. A versão do Jinja2 vai ser atualizada automaticamente e deve ser feito o downgrade para a versão para ser suportada pela versão 3.2.2 do SPADE:

pip install --upgrade --force-reinstall "Jinja2==3.0.1"